# Build artifacts
*.o
*.bin
*.riscv
*.dump
*.elf
*.asm
*.slm

# Sub-build dir
pmu_firmware/build/

# Generated by two-pass extract_addrs.py
auto_func_addrs.h

# Empty placeholder
fat_test.dump

# The FAT16 image IS tracked here: it is embedded into the ELF by
# fat_image_wrapper.S and the build fails without it.
!my_fat_image_reduced.img

# Known-good binaries from the verified session are tracked.
!prebuilt/
!prebuilt/**
